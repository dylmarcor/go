import React from 'react';
import './Board.css';

interface MyProps{};

const Board = (props: MyProps) => {

    return (
        <div>
            Board will be here!
        </div>
    )
}

export default Board;